
 <div class="reportSection">
    <h4>Size</h4>
    <p>
      The {{#isCollection}}collection{{/isCollection}}{{^isCollection}}zone{{/isCollection}} is <strong>{{size}} sq. km.</strong>
      
    </p>
 </div>

 <div class="reportSection">
    <h4>Minimum Dimension</h4>
    <p>
      The width of the smallest side of {{min_dim_name}} is <strong>{{min_dim_size}} km</strong>.
    </p>
 </div>

 <div class="reportSection">
    <h4>Displaced Fishing Value</h4>
    <p>
      The {{#isCollection}}collection{{/isCollection}}{{^isCollection}}sketch{{/isCollection}} displaces approximately <strong>{{displaced_fishing_value}}% of the total fishing value.</strong>
    </p>
 </div>

 <div class="reportSection">
    <h4>Displaced Dive Value</h4>
    <p>
      The {{#isCollection}}collection{{/isCollection}}{{^isCollection}}sketch{{/isCollection}} displaces approximately <strong>{{displaced_dive_value}}% of the total dive value.</strong>
    </p>
 </div>


